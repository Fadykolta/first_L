name: Heloo world workflow

on:
 push:
   branches: [ "main" ]
 pull_request:
   branches: [ "main" ]

 workflow_dispatch:
   

jobs:
 Hello_world:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v3
    - name: run multi-lines script
      runs: echo "Hello, World from ubuntu"  
      runs: mkdir Hello_world
      run: ls -a