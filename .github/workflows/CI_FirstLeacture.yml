name: Hello world basic

on:

  push:
    branshes: [ "main" ]
  pull_request:
    branshes: [ "main" ]


 workflow_dispatch:

jobs:
  Hello world:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: run a one-line script
        run: echo "Hello ya world"

