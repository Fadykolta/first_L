---
name: testing env
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

  workflow_dispatch: null

jobs:
  Build_ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run a one-line script
        run: echo Hello world ubuntu
      - name: run a multi-lines script
        run:
          ls -a
          touch "test1234156789">>ttt.txt
          ls -a
